import { FC } from "react"
import { Layout } from "antd"

const { Footer } = Layout

const AppFooter: FC = () => {
  return <Footer>React MQTT App @2023 Created by Trung</Footer>
}

export default AppFooter
